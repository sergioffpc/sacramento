# Former document-control records

Status: Historical reference, not active governance. These files preserve the
exact pre-reform inventory data and recorded evidence/control text from revision
`29d37ca`. Paths and statuses inside are historical and must not be interpreted as
current source authority. Do not modify accepted records or delete retained
evidence. [ADR-0014](../../adr/0014-use-lightweight-executable-documentation.md)
replaces the administrative workflow; the [migration map](../../project/documentation-migration.csv)
identifies retired process obligations.

The original control Markdown is retained as `.txt` so its historical navigation
and instructions cannot masquerade as the current workflow. Prior verification
criteria remain in the current acceptance catalogue; resolved ambiguity records
remain in the adjacent historical CSV. Older versions remain in Git history.
